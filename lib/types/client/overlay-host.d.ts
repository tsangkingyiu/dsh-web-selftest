import type { ReactNode } from 'react';
export declare function renderOverlay(node: ReactNode): void;
/** Remove the overlay from the page entirely (used when closed with no session). */
export declare function clearOverlay(): void;
//# sourceMappingURL=overlay-host.d.ts.map