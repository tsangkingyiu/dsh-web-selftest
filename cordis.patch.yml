# Bundle layer for dsh-web-selftest.
# A later patch that changes this row's config must restate every retained key.
- insert:
    - id: "web-selftest"
      name: "dsh-web-selftest"
      config:
        defaultMode: upper
        maxLength: 10000
